{
  "name": "notes-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "@redocly/cli": "^2.48.0",
    "@types/node": "^26.4.0",
    "typescript": "^5.5.2",
    "vitest": "~4.1.0",
    "wrangler": "^4.126.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
